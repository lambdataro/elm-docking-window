{
  "name": "elm-docking-window",
  "version": "1.0.0",
  "description": "Elmでドッキングウインドウを作ってみるリポジトリ",
  "private": true,
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "rimraf dist && npm run build && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lambdataro/elm-docking-window.git"
  },
  "author": "lambdataro",
  "license": "MIT",
  "devDependencies": {
    "elm-hot": "^1.1.5",
    "gh-pages": "^3.1.0",
    "node-elm-compiler": "^5.0.5",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2"
  }
}